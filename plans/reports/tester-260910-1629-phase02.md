# Phase 02 Auth/Users/Profiles Test Report — 2026-09-11

## Scope

Inspected the Phase 02 plan and latest wrapper/throttle changes. Added/extended only tests under `test/` and `src/auth/*.spec.ts`; no production, schema, package, or environment files were changed or printed.

## Commands

| Command | Exit | Result |
|---|---:|---|
| `pnpm exec prisma validate` | 0 | Schema valid. |
| `pnpm run build` | 0 | Nest production compilation passed. |
| `pnpm exec vitest run --exclude 'spec/e2e/**'` | 0 | 4 files, 10 tests passed. Duration 783ms. |
| `pnpm run lint` | 0 | 28 files, 0 errors, 22 warnings. |
| `pnpm run test:e2e` | 0 | 3 files, 13 tests passed, including repaired empty update wrapper. Duration 8.12s. |

## Integration/database status

The E2E config loads dotenv, requires `TEST_DATABASE_URL`, sets `NODE_ENV=test`, and the application maps `DATABASE_URL` to that test value. The live tests used Prisma through Nest and generated unique `e2e_*`/`e2e_profile_*` usernames; teardown deleted only users with those prefixes. No development database was used and credentials were not printed.

## Findings

- The prior DTO initialization defect is resolved.
- Live coverage exercises registration/login/current user, settings, anonymous/authenticated profiles, malformed Token 401, password-token invalidation, duplicate email/username conflicts, unknown profile 404, missing/null wrappers, and client-wide registration throttling.
- The empty update wrapper repair is verified: `PUT /api/user` with `{ user: {} }` passes the expected 422 assertion.
- Unit throttle tests pass (10 focused tests total), including distinct accounts sharing a client and expired-entry eviction.
- Lint passed with 22 warnings, including auth/users/profiles repository-pattern warnings, generic `Error` warnings, JWT expiration heuristic warning, cache-header warning, and magic constants/strings.
- E2E config requires and loads `TEST_DATABASE_URL`, sets `NODE_ENV=test`, and runtime maps the database URL to that test value. No env value was printed and no development database was used.
- `.env.example` was not read in this run; no environment values were exposed.

**Status:** DONE
