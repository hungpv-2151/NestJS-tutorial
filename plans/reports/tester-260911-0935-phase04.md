# Phase 04 Social/Comments Test Report — 2026-09-11

## Scope

Added live integration tests in `test/social.e2e-spec.ts` and `test/comments.e2e-spec.ts`. Tests use the configured `TEST_DATABASE_URL` path, generated unique prefixes, and scoped cleanup. No source, schema, config, package, or environment files were changed.

## Commands and Results

| Command | Exit | Result |
|---|---:|---|
| `pnpm run test:e2e` | 0 | 7 files, 28 tests passed. Duration 9.90s. |
| `pnpm exec vitest run --exclude 'spec/e2e/**'` | 0 | 4 files, 10 tests passed. Duration 856ms. |
| `pnpm run build` | 0 | Nest compilation passed. |
| `pnpm run lint` | 0 | 39 source files, 0 errors, 62 warnings. |

## E2E Findings

- Follow and favorite POST endpoints now return HTTP 200 with the expected envelopes.
- Article update responses include the expected `article` envelope.
- Comment tests pass, including public list, authenticated create, owner-scoped delete, non-owner 403 with data preserved, missing article/comment 404, and validation errors.
- Deterministic concurrent-removal coverage passes: unfollow after target deletion returns profile 404; favorite after article deletion returns article 404. A true mid-transaction P2003 race is not reproducible safely through this HTTP test harness.

## Passing Coverage

The new social tests exercise idempotent follow/unfollow and favorite/unfavorite intent, self/unknown profile cases, favorite counts, JWT-required feed, pagination, followed-author filtering, and serialization. Comment cleanup deletes only generated article/user prefixes.

## Warnings

Lint exits 0 with 62 warnings, mainly direct Prisma access, transaction/repository heuristics, caught-error handling, magic constants, and existing auth/config warnings.

**Status:** DONE
