# Phase 03 Articles/Tags Test Report — 2026-09-11

## Scope

Added live integration tests in `test/articles.e2e-spec.ts` and `test/tags.e2e-spec.ts`. Tests use the E2E `TEST_DATABASE_URL` path, generated unique prefixes, and teardown limited to those prefixes. No source, schema, package, config, or environment files were changed.

## Commands and Results

| Command | Exit | Result |
|---|---:|---|
| `pnpm run test:e2e` | 0 | 5 files, 20 tests passed. Duration 11.27s. |
| `pnpm exec vitest run --exclude 'spec/e2e/**'` | 0 | 4 files, 10 tests passed. Duration 698ms. |
| `pnpm run build` | 0 | Nest compilation passed. |
| `pnpm run lint` | 0 | 35 source files, 0 errors, 37 warnings. |

## Final E2E Contract Checks

Previous null-tag validation and tag-only ownership defects are fixed. Final retest is green, including `test/articles.e2e-spec.ts` — `updates tags, rejects null tags, and enforces ownership`.

- Request: author `PUT /api/articles/:slug` with `{ article: { tagList: [] } }` after creating an article.
- Result: HTTP 200 with cleared tags.

## Coverage

Passing live tests cover article create/detail/list shape, ordered tags, tag and author filters, stable offset pages, update/clear tags, ownership 403, unknown article 404, invalid input, and sorted tag listing. Cleanup runs with generated username/tag prefixes.

Final retest additionally verifies repeated-title slug uniqueness with bounded collision fallback, zero-row update/delete behavior after removal (TOCTOU path), and `favorited: false`/`favoritesCount: 0` serializer fields.

## Warnings

Lint exits 0 but reports 37 warnings, including direct Prisma access in article/tag services, caught-error handling in article creation, JWT heuristic warning, and existing auth/config warnings.

**Status:** DONE
